export const baseUrl = `https://ddragon.leagueoflegends.com/cdn`;
export const version = `12.8.1`;
export const region = `zh_TW`;

export const config = {
  championsUrl: `${baseUrl}/${version}/data/${region}/champion.json`,
  championUrl: `${baseUrl}/${version}/data/${region}/champion/`,
  championSkinUrl: `${baseUrl}/img/champion/splash/`,
};
